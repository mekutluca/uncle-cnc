/** Nameplate (plaka) form alanları için ortak sınıflar — native select/radio/file
 *  elemanları da shadcn Input ile aynı dile oturur. */
export const labelClass =
	'font-mono text-[11px] font-medium uppercase tracking-[0.18em] text-muted-foreground';

export const selectClass =
	'border-input bg-card h-10 w-full min-w-0 rounded-md border px-3 text-sm shadow-xs ' +
	'focus-visible:ring-ring/50 focus-visible:border-ring focus-visible:ring-2 outline-none ' +
	'disabled:cursor-not-allowed disabled:opacity-50';
